/** Root react-grid-layout row height — must match TabGroup inner row height for height sync. */
export const DASHBOARD_GRID_ROW_HEIGHT_PX = 80;

/** Gap between cells in tab group inner CSS grid (matches former RGL margin feel). */
export const TAB_GROUP_GRID_GAP_PX = 8;

/** Inner tab-group grid uses the same row height as root for consistent parent `h` conversion. */
export const TAB_GROUP_INNER_ROW_HEIGHT_PX = DASHBOARD_GRID_ROW_HEIGHT_PX;

export const TAB_GROUP_INNER_COLS = 12;
